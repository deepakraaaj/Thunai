# PROPOSALS (cross-lane requests, human approves)
[01:52] [codex] Human/other-lane update required: absorb the official PromptWars rules from the supplied “UPDATE ALL COORDINATION MD FILES” brief into root AGENTS.md, CLAUDE.md, GEMINI.md, .agents/rules/lane-antigravity.md, .agents/rules/promptwars-directives.md, and PARALLEL-EXECUTION.md without changing ownership, worktrees, the contracts freeze, or STATUS/PROPOSALS protocols. Root AGENTS.md must add live Cerebras→Gemini Flash-Lite generation, backend-only keys/proxying, explicitly labeled offline scripts, response transparency metadata, input-only “Load test scenario,” strict TypeScript/ESLint/Prettier standards, README requirements, endpoint security/consent controls, and hour-five lane tests/accessibility. Replace every “Seed Demo Data”/“Demo Mode” reference. PARALLEL-EXECUTION checkpoint 3 must require public deployment and a phone smoke test; its final hour must remove broken UI. Antigravity pitch rules must map features to the problem-statement phrases and invite evaluator free-roam. Claude must create and continuously maintain root SUBMISSION.md with the specified feature→GenAI→code mapping, role credentials, deployed URLs, and local steps. These targets are outside Codex ownership.
[00:42] [claude] MODEL ID CORRECTION (verified live against the provided keys): the failover model named in the brief, Gemini `gemini-2.5-flash-lite`, returns HTTP 404 "no longer available to new users" via generativeLanguage generateContent with our key. Working, stable Flash-Lite equivalent for this key is `gemini-flash-lite-latest` (verified 200, ~1.8s). packages/core is built to use `gemini-flash-lite-latest` as the failover modelId (still Gemini Flash-Lite family, per AGENTS.md). Cerebras primary verified as `gpt-oss-120b`. No cross-lane action needed unless a human wants a different pinned Gemini id — flagging so apps/api uses the same env value (GEMINI_MODEL) rather than a hardcoded 404 id.
