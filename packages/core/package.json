{
  "name": "@anchor/core",
  "version": "0.1.0",
  "description": "Anchor core domain logic: live provider router (Cerebras→Gemini failover), prompt templates, crisis-safety layer, risk-window prediction, and scenario seed data.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anchor/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
