# Anchor

- **v1/** — warm-up: problem decode & approach (early exploration under repo root).
- **v2/** — the application (see [v2/README.md](v2/README.md)).

> Deploy note: set the Vercel **Root Directory** to `v2`.
